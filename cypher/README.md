# openCypher reference

This folder is reserved for parameterized Cypher fragments and reusable queries used by the application.

Planned content:

- role fit analysis
- skill adjacency and pathfinding
- graph subgraph materialization for UI rendering
- project recommendation queries

All user input must be passed as query parameters.
